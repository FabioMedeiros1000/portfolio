export type FooterProps = {
    logo: string;
    text: string;
    link: string;
    alt: string;
};
declare const FooterGroup: ({ logo, text, link, alt }: FooterProps) => import("react/jsx-runtime").JSX.Element;
export default FooterGroup;
